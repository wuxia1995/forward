<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ntech.dao.InfoMapper">
  <resultMap id="Info" type="com.ntech.model.Info">
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="name" jdbcType="VARCHAR" property="Name" />

    <result column="content" jdbcType="VARCHAR" property="content" />
    <result column="result" jdbcType="INTEGER" property="result" />
    <result column="time" jdbcType="DATE" property="time"/>
  </resultMap>
  <insert id="insert" parameterType="com.ntech.model.Info">
      INSERT INTO info(name,content,result,time)
      VALUES (#{name,jdbcType=VARCHAR},
      #{content,jdbcType=VARCHAR},
      #{result,jdbcType=INTEGER},
      #{time,jdbcType=DATE})
  </insert>
  <!-- 根据姓名查日志-->
  <select id="findByName" parameterType="java.lang.String" resultMap="Info">
    SELECT id, content,result,time FROM  info where name=#{name,jdbcType=VARCHAR}
  </select>

</mapper>